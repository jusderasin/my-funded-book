-- Formulaire de trade complet + synchronisation de données entre appareils.
-- À exécuter une fois dans Supabase > SQL Editor.

alter table public.tradesj'ades_account_id_idx on public.trades(account_id);
